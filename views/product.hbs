<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Danh sách sản phẩm</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1 class="my-4">Danh sách sản phẩm hiện có</h1>
        <div class="row">
            {{#each products}}
            <div class="col-md-4">
                <div class="card mb-4">
                    <img class="card-img-top" src="{{this.img}}" alt="{{this.name}}">
                    <div class="card-body">
                        <h5 class="card-title">{{this.name}}</h5>
                        <p class="card-text">{{this.price}} VND</p>
                        <a href="/product/edit/{{this._id}}" class="btn btn-primary">Sửa</a>
                        <form action="/product/delete/{{this._id}}" method="POST" class="d-inline">
                            <button type="submit" class="btn btn-danger">Xóa</button>
                        </form>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        <a href="/product/add" class="btn btn-success">Thêm sản phẩm</a>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
